# A股策略回测工作台项目说明书

## 1. 目标

本项目是一个真实可运行的 Windows 桌面版 A 股回测工具，不是单纯的静态页面。当前交付目标包括：

1. 用本地历史数据完成全市场或指定股票池回测。
2. 支持用户录入入场条件、离场条件，并在运行前完成接口级校验。
3. 首页同时展示指数、红绿家数、强势板块、昨日强势追踪、行情评价、新闻汇总、资讯、同花顺复盘/早盘总评和风险清单。
4. 跑完策略后，收益概览直接列出当日符合策略的股票，便于把回测信号和行情主线对齐。
5. 所有业务数据统一落在 `D:\New project 6` 目录树，不再把业务数据写到 `AppData`；`C:\Users\大帝之资\Documents\New project 6` 仅作为指向 D 盘目录的 Junction 入口。

### 1.1 文档分工

1. `README.md` 面向 GitHub 展示，只保留项目简介、核心能力、通用架构、主要接口和开发命令。
2. 本说明书负责记录项目细节，包括本机路径、运行产物、按钮与接口映射、上游数据源、保存策略规则、验证命令和清理边界。
3. 凡是与本机安装路径、磁盘位置、签名密钥、安装包、数据仓、Junction 有关的内容，都应维护在本说明书或 `运行产物\目录说明.md`，不要放回 `README.md`。

## 2. 统一路径原则

### 2.1 真实业务根目录

- `D:\New project 6`
- `C:\Users\大帝之资\Documents\New project 6` 是 Junction，不保存第二份项目文件。

### 2.2 业务数据与运行产物目录

- 本地数据仓：`D:\New project 6\运行产物\本地数据仓`
- 项目缓存联接：`D:\New project 6\.astock-cache`
- 已保存策略：`D:\New project 6\运行产物\策略配置\saved-strategies.json`
- 安装包：`D:\New project 6\运行产物\安装包`
- 签名密钥：`D:\New project 6\运行产物\签名密钥`

### 2.3 路径规则说明

1. `.astock-cache` 只是指向 `运行产物\本地数据仓` 的 Junction。
2. 桌面端本地数据服务启动后，`cache_dir` 和 `/health` 返回的 `cache_path` 应落在 `D` 盘目录树。
3. 发布态桌面端不再把历史数据或策略配置当成 `AppData` 业务数据。
4. 桌面端保存策略时，正式落地文件是 `运行产物\策略配置\saved-strategies.json`。
5. 不在 C 盘保留项目副本；如果需要从“文档”目录打开项目，应继续使用现有 Junction。

## 3. 总体架构

项目分为三层：

1. 前端界面层：`frontend/src`，React + TypeScript
2. 桌面容器层：`src-tauri/src`，Tauri + Rust
3. 数据与回测层：`backend/astock_backtester`，Python

运行链路：

1. 前端先通过 Tauri 命令 `ensure_data_service` 拉起本地服务。
2. Tauri 启动 sidecar `astock-data-service.exe` 或 Python 模块服务。
3. 前端通过 `http://127.0.0.1:<port>` 调用本地 HTTP 服务。
4. 数据服务从本地数据仓读取历史数据，并聚合实时行情、资讯、风险信息。
5. 回测执行完成后，把阶段进度、交易事件和最终结果返回给前端。

## 4. 目录说明

### 4.1 源码目录

- `backend`：Python 后端、数据服务、回测逻辑
- `frontend`：React 前端
- `src-tauri`：Tauri 命令、桌面进程管理、更新配置
- `tests`：Python 开发验证用例，保留在仓库中用于防止接口和回测逻辑回归
- `scripts`：构建与发布脚本

### 4.2 运行目录

- `运行产物\本地数据仓`：历史数据与本地仓库
- `运行产物\策略配置`：用户保存的策略配置
- `运行产物\安装包`：安装包与发布产物
- `运行产物\签名密钥`：更新签名密钥

## 5. 数据设计

### 5.1 历史数据

历史数据主要有两种形态：

1. 旧缓存：`LocalCache`
2. 新仓库：`Warehouse`

当前实现规则：

1. 导入或补齐日线时，同时写 `LocalCache` 和 `Warehouse`
2. 覆盖率查询优先读 `Warehouse`
3. 回测优先读 `Warehouse`
4. `Warehouse` 不够时才退回 `LocalCache`

### 5.2 本地服务为何必须走本地仓

原因有三点：

1. 回测结果要可复现，不能在回测过程中依赖外网波动
2. 全市场数据量大，不能每次临时拉取
3. 红绿家数、昨日强势追踪等模块需要保留本地历史兜底，但优先使用可用的实时上游

## 6. Tauri 命令

### 6.1 `ensure_data_service`

用途：

1. 启动或复用本地数据服务
2. 返回当前端口、基础 URL、缓存目录

代码入口：

- `src-tauri/src/commands.rs`
- `src-tauri/src/service_manager.rs`

### 6.2 `backend_command`

用途：

1. 调用 Python CLI
2. 目前主要用于浏览器预览或无数据服务时的回退路径

### 6.3 `load_saved_strategies`

用途：

1. 桌面端启动后读取已保存策略
2. 读取文件：`D:\New project 6\运行产物\策略配置\saved-strategies.json`

### 6.4 `persist_saved_strategies`

用途：

1. 桌面端保存或删除自定义策略
2. 写入文件：`D:\New project 6\运行产物\策略配置\saved-strategies.json`

## 7. 本地 HTTP 服务接口

接口实现文件：

- `backend/astock_backtester/service.py`

### 7.1 GET 接口

| 接口 | 作用 | 后端实现 |
| --- | --- | --- |
| `GET /ping` | 轻量健康探测 | `do_GET` |
| `GET /health` | 返回缓存路径、端口、覆盖信息 | `build_service_health(...)` |
| `GET /logs/recent` | 最近日志 | `self.server.state.logs` |
| `GET /realtime/market-snapshot` | 首页行情快照 | `RealtimeMarketProvider.market_snapshot()` |
| `GET /market/news` | 首页资讯 | `MarketNewsProvider.latest_news()` |
| `GET /market/commentary` | 独立行情评价 | `MarketCommentaryProvider.current_commentary()` |
| `GET /market/news-summary` | 独立新闻汇总 | `MarketNewsSummaryProvider.latest_summary()` |
| `GET /market/fupan` | 同花顺复盘总评 | `MarketBriefingProvider.latest_fupan()` |
| `GET /market/zaopan` | 同花顺早盘总评 | `MarketBriefingProvider.latest_zaopan()` |
| `GET /risk/alerts` | 风险清单 | `RiskAlertProvider.current_alerts()` |
| `GET /strategy/recommended` | 推荐策略模板 | `recommended_strategies(...)` |
| `GET /sync/jobs/{job_id}` | 全市场同步进度 | `SyncJobManager.get_job(...)` |

### 7.2 POST 接口

| 接口 | 作用 | 后端实现 |
| --- | --- | --- |
| `POST /coverage/daily-bars` | 查询指定股票和日期区间的覆盖细节 | `build_daily_bars_coverage(...)` |
| `POST /sync/full-market` | 启动全市场历史下载 | `SyncJobManager.start_full_market(...)` / `run_full_market(...)` |
| `POST /import/daily-bars` | 导入示例数据或本地文件 | `import_daily_bars_into_cache(...)` |
| `POST /fetch/daily-bars` | 补齐缺失行情 | `fetch_daily_bars_into_cache(...)` |
| `POST /run/backtest` | 普通回测 | `run_configured_backtest(...)` |
| `POST /run/backtest/stream` | 流式回测 | `_run_backtest_stream(...)` |
| `POST /strategy/conditions/validate` | 校验入场/离场条件文本 | `validate_condition_text(...)` / `validate_exit_condition_text(...)` |

## 8. 页面按钮与接口映射

这一节只写“当前页面里真正存在的按钮和模块”。

### 8.1 应用启动阶段

| 页面 / 组件 | 动作 | 前端函数 | 调用接口 | 后端入口 |
| --- | --- | --- | --- | --- |
| `App` / `DataCenter` | 启动即连接本地服务 | `ensureDataService(cacheDir)` | Tauri `ensure_data_service` | `DataServiceManager::ensure_running` |
| `App` | 首次加载推荐策略 | `loadRecommendedStrategies` | `GET /strategy/recommended` | `recommended_strategies(...)` |
| `App` | 首次加载资讯 | `loadMarketNews` | `GET /market/news` | `MarketNewsProvider.latest_news()` |
| `App` | 首次加载行情评价 | `loadMarketCommentary` | `GET /market/commentary` | `MarketCommentaryProvider.current_commentary()` |
| `App` | 首次加载新闻汇总 | `loadNewsSummary` | `GET /market/news-summary` | `MarketNewsSummaryProvider.latest_summary()` |
| `App` | 首次加载同花顺复盘总评 | `loadMarketBriefing(..., "fupan")` | `GET /market/fupan` | `MarketBriefingProvider.latest_fupan()` |
| `App` | 首次加载同花顺早盘总评 | `loadMarketBriefing(..., "zaopan")` | `GET /market/zaopan` | `MarketBriefingProvider.latest_zaopan()` |
| `App` | 首次加载风险 | `loadRiskAlerts` | `GET /risk/alerts` | `RiskAlertProvider.current_alerts()` |
| `App` | 首次加载行情 | `loadRealtimeMarketSnapshot` | `GET /realtime/market-snapshot` | `RealtimeMarketProvider.market_snapshot()` |
| `App` | 首次加载已保存策略 | `loadSavedStrategiesFromStore` | Tauri `load_saved_strategies` | 读取 `saved-strategies.json` |

### 8.2 数据中心

组件文件：

- `frontend/src/components/DataCenter.tsx`

| 按钮 / 模块 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 刷新覆盖范围 | `handleRefreshDetails` | `/coverage/daily-bars` | `POST` | 查询当前股票列表在日期区间内的行情、资金流、市值缺口 |
| 刷新覆盖范围后同步状态 | `refreshServiceState` | `/health` | `GET` | 刷新整体数据覆盖与缓存路径 |
| 刷新覆盖范围后同步日志 | `refreshLogs` | `/logs/recent` | `GET` | 刷新本地服务日志 |
| 下载全市场历史数据 | `handleFullMarketSync` | `/sync/full-market` | `POST` | 启动全市场导入任务 |
| 同步进度轮询 | `loadSyncJob` | `/sync/jobs/{job_id}` | `GET` | 每秒轮询一次进度 |
| 补全缺失数据 | `handleFetch` | `/fetch/daily-bars` | `POST` | 对当前股票列表补齐日线 |
| 导入示例数据 | `handleImportSample` | `/import/daily-bars` | `POST` | `source=sample` |
| 导入本地文件 | `handleImportFile` | `/import/daily-bars` | `POST` | `source=file, path=<文件路径>` |

### 8.3 首页行情、资讯、风险

组件文件：

- `frontend/src/components/MarketDashboard.tsx`
- `frontend/src/components/MarketCommentaryPanel.tsx`
- `frontend/src/components/NewsSummaryPanel.tsx`
- `frontend/src/components/NewsPanel.tsx`
- `frontend/src/components/TonghuashunBriefingPanel.tsx`
- `frontend/src/components/RiskAlertsModal.tsx`

| 模块 / 按钮 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 今日实时行情 | `loadRealtimeMarketSnapshot` | `/realtime/market-snapshot` | `GET` | 指数、红绿家数、强势板块、昨日强势追踪统一走这里 |
| 行情评价 | `loadMarketCommentary` | `/market/commentary` | `GET` | 独立展示结论、主线、风险、明日观察，不替代“今日实时行情” |
| 新闻汇总 | `loadNewsSummary` | `/market/news-summary` | `GET` | 独立展示新闻主题、要点、风险和来源条数，不替代“资讯与事件” |
| 资讯与事件 | `refreshNews` | `/market/news` | `GET` | 支持定时刷新与手动刷新 |
| 同花顺复盘总评 | `loadMarketBriefing(baseUrl, "fupan")` | `/market/fupan` | `GET` | 独立显示在行情/资讯框下方，不再嵌入“今日实时行情” |
| 同花顺早盘总评 | `loadMarketBriefing(baseUrl, "zaopan")` | `/market/zaopan` | `GET` | 独立显示在行情/资讯框下方，不再嵌入“资讯与事件” |
| 查看同花顺复盘总评全文 | `setOpenKind("fupan")` | 无新增接口 | 无 | 点击按钮不再发起新的前端请求；`/market/fupan` 首次加载时已由后端抓取并展开文章详情，弹窗展示已返回的摘要、`sections`、`links`、`tables` 和诊断信息 |
| 查看同花顺早盘总评全文 | `setOpenKind("zaopan")` | 无新增接口 | 无 | 点击按钮不再发起新的前端请求；`/market/zaopan` 首次加载时已由后端抓取并展开文章详情，弹窗展示已返回的摘要、`sections`、`links`、`tables` 和诊断信息 |
| 同花顺原文按钮 | `<a href={briefing.source_url}>` | 同花顺网页 | 外链 | 打开 `stock.10jqka.com.cn/fupan/` 或 `stock.10jqka.com.cn/zaopan/` |
| 风险提示弹窗刷新 | `refreshRiskAlerts` | `/risk/alerts` | `GET` | ST / 退市风险清单 |
| 风险提示 summary 卡片 | `setRiskModalOpen(true)` | 无 | 无 | 只是打开弹窗，不额外打接口 |
| 风险弹窗关闭 | `onClose` | 无 | 无 | 纯前端状态切换 |

当前布局规则：

1. `MarketDashboard` 只负责“今日实时行情”，展示指数、红绿家数、强势板块和昨日强势追踪；不承载同花顺复盘/早盘，也不再展示“收盘后板块解读”。
2. `MarketCommentaryPanel` 只负责“行情评价”，消费 `/market/commentary` 的 `summary`、`drivers`、`risks`、`next_watch`。
3. `NewsSummaryPanel` 只负责“新闻汇总”，消费 `/market/news-summary` 的 `themes`、`highlights`、`risks`、`item_count`。
4. `NewsPanel` 只负责“资讯与事件”，不承载同花顺早盘，也不承载新闻主题汇总。
5. `TonghuashunBriefingPanel` 单独承载“同花顺复盘总评”和“同花顺早盘总评”两张卡片，接口失败时显示各自的兜底提示。
6. 同花顺卡片里的“查看全文”不重新请求接口，而是展开后端已返回的文章详情结构；弹窗内按“重点摘要 / 阅读全文”组织长文本，原文按钮才跳转到外部网页。

### 8.4 策略配置

组件文件：

- `frontend/src/components/StrategyWorkbench.tsx`
- `frontend/src/components/RecommendedStrategies.tsx`

| 按钮 / 模块 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 校验入场条件 | `onValidateCondition` | `/strategy/conditions/validate` | `POST` | `mode=entry` |
| 校验离场条件 | `validateExitCondition` | `/strategy/conditions/validate` | `POST` | `mode=exit` |
| 添加入场条件 | `addValidatedCondition` | 无 | 无 | 使用刚刚校验成功的结果写入前端策略状态 |
| 添加离场条件 | `addValidatedExitCondition` | 无 | 无 | 使用刚刚校验成功的结果写入前端策略状态 |
| 套用推荐策略 | `onApply(item.strategy)` | 无 | 无 | 推荐策略数据本身来自 `/strategy/recommended` |
| 套用已保存策略 | `onApplySavedStrategy` | 无 | 无 | 纯前端状态覆盖当前策略 |
| 删除已保存策略 | `persistSavedStrategiesToStore` | Tauri `persist_saved_strategies` | invoke | 写回 `saved-strategies.json` |
| 重置策略 | `resetStrategy` | 无 | 无 | 重置为默认策略 |
| 套用数据中心日期 | `applyCoverageDates` | 无 | 无 | 直接把覆盖日期写入回测设置 |

### 8.5 收益概览与回测

组件文件：

- `frontend/src/components/ResultsOverview.tsx`

| 按钮 / 模块 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 运行历史回测 | `runBacktest` | `/run/backtest/stream` | `POST` | 桌面端主路径 |
| 回测流式事件 | `runBacktestStreamWithDataService` | `/run/backtest/stream` | `POST` | 返回 phase / progress / trade / result |
| 浏览器预览回退 | `runConfiguredBacktest` | Tauri `backend_command` | invoke | 仅无本地服务时使用 |
| 当日符合策略股票 | `latest_strategy_matches.matches` | `/run/backtest/stream` 最终 result | `POST` | 后端在结果里返回最新交易日命中股票，前端展示代码、名称、收盘价、涨跌幅、命中原因 |
| 查看风险清单 | `onOpenRiskAlerts` | 无 | 无 | 仅打开风险弹窗 |
| 回测完成后询问保存策略 | `window.confirm(...)` | 无 | 无 | 用户确认后再调用 Tauri `persist_saved_strategies` |

结果字段说明：

1. `BacktestResult.latest_strategy_matches.signal_date`：命中信号日期。
2. `BacktestResult.latest_strategy_matches.trade_date`：用于展示的交易日期。
3. `BacktestResult.latest_strategy_matches.matches[]`：当日符合策略的股票列表。
4. `matches[].symbol/name/close/change_pct/reasons/rank_score`：前端“当日符合策略股票”卡片的展示字段。
5. 前端保留 `matched_stocks` 兼容回退，但正式后端字段以 `latest_strategy_matches.matches` 为准。

### 8.6 更新面板

组件文件：

- `frontend/src/components/UpdatePanel.tsx`
- `frontend/src/updateClient.ts`

| 按钮 | 前端函数 | 调用接口 | 说明 |
| --- | --- | --- | --- |
| 检查更新 | `checkForUpdate` | Tauri updater `check()` | 读取 GitHub Release `latest.json` |
| 安装并重启 | `installAndRelaunch` | Tauri updater `downloadAndInstall()` + process `relaunch()` | 不经过本地 HTTP 服务 |

## 9. 行情与资讯的上游来源

### 9.1 `GET /realtime/market-snapshot`

后端文件：

- `backend/astock_backtester/data/realtime.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 指数行情 | `_fetch_indexes()` | Sina `https://hq.sinajs.cn/list=...` |
| 强势板块主链 | `_fetch_ths_concept_section_rows()` | 同花顺概念题材页 `q.10jqka.com.cn/gn/` 内嵌 `gnSection` 概念板块行情 |
| 强势板块一级回退 | `_fetch_sector_rows()` | 东方财富概念板块 `push2.eastmoney.com/api/qt/clist/get` |
| 强势板块二级回退 | `_fetch_ths_industry_html_rows()` | 同花顺行业页 `q.10jqka.com.cn/thshy/index/field/199112/order/desc/page/{page}/` |
| 强势板块三级回退 | `_fetch_sector_rows()` | 东方财富行业板块 `push2.eastmoney.com/api/qt/clist/get` |
| 强势板块四级回退 | `_fetch_sina_sectors()` | Sina 板块页 |
| 强势板块末级题材标签兜底 | `_fetch_ths_hot_topic_rows()` | 同花顺热点归因 `zx.10jqka.com.cn/event/api/getharden/...`，只在真实板块行情不可用时使用 |
| 红绿家数主链 | `_fetch_live_breadth()` | 同花顺市场总览页 `q.10jqka.com.cn/index/index/board/all/` |
| 红绿家数一级回退 | `_fetch_live_breadth()` | 东方财富全 A 按市场段分页聚合统计 `82.push2.eastmoney.com/api/qt/clist/get` |
| 红绿家数二级回退 | `_snapshot_from_local()` | 本地 `Warehouse` 最近交易日 |
| 昨日强势追踪 | `_snapshot_from_local()` + `_local_market_groups()` | 当前实时板块成员 + 本地 `Warehouse` 前一交易日 |
| 本地强势板块兜底 | `_local_market_groups()` | 对实时板块成分股按本地历史涨跌幅聚合 |

### 9.2 `GET /market/news`

后端文件：

- `backend/astock_backtester/data/news.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 东方财富栏目资讯 | `_fetch_eastmoney_columns()` | `np-listapi.eastmoney.com/comm/web/getNewsByColumns` |
| 东方财富要闻页 | `_fetch_eastmoney_rolling()` | `finance.eastmoney.com/yaowen.html` |
| 财联社电报 | `_fetch_cailianpress()` | `www.cls.cn/nodeapi/telegraphList` |

### 9.3 `GET /market/commentary`

后端文件：

- `backend/astock_backtester/data/market_commentary.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 行情评价基础盘面 | `current_commentary()` | `RealtimeMarketProvider.market_snapshot()` |
| 新闻催化辅助 | `_news_theme_text()` | `MarketNewsProvider.latest_news(limit=12)` |
| 主线驱动 | `MarketCommentaryPoint(...)` | 强势板块、红绿家数、新闻标签聚合 |
| 风险与明日观察 | `current_commentary()` | 指数涨跌、红绿家数、强势题材延续情况 |
| 降级兜底 | `current_commentary()` | 实时行情不可用时返回防守口径与诊断 |

前端显示规则：

1. `MarketCommentaryPanel` 独立展示“行情评价”，不改变“今日实时行情”的原始快照展示。
2. 字段 `stance` 映射为偏强 / 中性 / 防守；字段 `summary` 展示为“结论”。
3. 字段 `drivers` 展示为“主线”，字段 `risks` 展示为“风险”，字段 `next_watch` 展示为“明日观察”。

### 9.4 `GET /market/news-summary`

后端文件：

- `backend/astock_backtester/data/news_summary.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 新闻原始列表 | `latest_summary()` | `MarketNewsProvider.latest_news(limit=24)` |
| 主题分组 | `_theme_keys()` | 新闻 tags + 标题/摘要关键词 |
| 主题情绪 | `_theme_sentiment()` | 主题内新闻 sentiment 计数 |
| 主题摘要 | `_theme_summary()` | 主题内代表新闻与来源集合 |
| 重点标题 | `highlights` | 最近发布时间排序后的新闻标题 |
| 风险标题 | `risks` | negative sentiment 或 ST / 退市标签 |

前端显示规则：

1. `NewsSummaryPanel` 独立展示“新闻汇总”，不改变现有“资讯与事件”列表。
2. 字段 `item_count` 显示新闻总数；字段 `themes[].source_count` 显示来源条数。
3. 字段 `themes[].headlines` 展示为“要点”，字段 `risks` 展示为每个主题卡片的风险提示。

### 9.5 `GET /market/fupan` 与 `GET /market/zaopan`

后端文件：

- `backend/astock_backtester/data/briefing.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 同花顺复盘总评 | `latest_fupan()` / `_parse_fupan()` | `https://stock.10jqka.com.cn/fupan/` |
| 同花顺早盘总评 | `latest_zaopan()` / `_parse_zaopan()` | `https://stock.10jqka.com.cn/zaopan/` |
| 复盘/早盘链接 | `_links_from_node()` | 同花顺页面内的相对链接与绝对链接 |
| 文章详情展开 | `_expand_article_links()` / `_fetch_article_section()` | 对 `stock.10jqka.com.cn/*.shtml` 链接抓取正文并追加为 `全文：...` 章节 |
| 复盘/早盘表格 | `_table_from_node()` | 同花顺页面内表格 |
| 网络失败兜底 | `_fallback()` | 返回可显示的空状态与诊断信息 |

前端显示规则：

1. `/market/fupan` 和 `/market/zaopan` 由 `TonghuashunBriefingPanel` 独立消费。
2. 这两个接口不写入 `MarketDashboard` 和 `NewsPanel` 的内部布局，避免复盘/早盘总评挤占实时行情或资讯列表。
3. 卡片摘要只展示优先章节；点击“查看全文”后，弹窗按“重点摘要 / 阅读全文”展示后端已展开的 `sections`、`links`、`tables` 和诊断信息。
4. 长文本正文会按段落拆分，弹窗主体支持滚动，确保能读到全文尾部。
5. 接口不可用时前端保留框架和提示，行情、资讯、风险等模块仍按各自接口独立工作。

### 9.6 `GET /risk/alerts`

后端文件：

- `backend/astock_backtester/data/risk.py`

内部来源：

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 本地潜在风险观察名单 | `_alerts_from_local_watchlist()` | `potential_risk_watchlist.csv` |
| 东方财富风险源 | `_alerts_from_eastmoney()` | 东方财富 ST / 退市名称列表 |
| 新浪实时名称扫描 | `_alerts_from_sina_local_symbols()` | Sina 简版行情名称 |
| AData 兜底 | `_alerts_from_adata_all_codes_with_timeout()` | `adata.stock.info.all_code()` |
| 本地仓库兜底 | `_alerts_from_local()` | `Warehouse` 最近交易日与 `is_st` 字段 |

## 10. 策略保存与持久化规则

### 10.1 内置策略

内置策略定义在：

- `frontend/src/savedStrategies.ts`

当前保留：

1. 基础均衡策略
2. 放量突破策略
3. 回踩均线策略

### 10.2 用户保存策略

桌面端行为：

1. `App` 启动时通过 Tauri `load_saved_strategies` 读取 `saved-strategies.json`
2. 回测运行成功且用户确认保存时，通过 Tauri `persist_saved_strategies` 写回该文件
3. 删除已保存策略也写回同一个文件

浏览器预览 / 单测回退：

1. 非 Tauri 运行时仍允许使用 `localStorage`
2. 这只是预览与测试回退，不作为桌面端正式数据存储规则

## 11. 测试与验证

推荐验证命令：

```powershell
python -m pytest tests/test_data_operations.py tests/test_data_service_http.py tests/test_build_scripts.py -q
npm run test:ui -- --run
```

本次桌面端 1.0.3 更新的专项验证命令：

```powershell
python -m pytest tests/test_engine.py tests/test_market_briefing.py tests/test_market_commentary.py tests/test_news_summary.py tests/test_data_service_http.py
& 'C:\Users\大帝之资\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' 'D:\New project 6\node_modules\vitest\vitest.mjs' --config frontend/vitest.config.ts --run
& 'C:\Users\大帝之资\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' 'D:\New project 6\node_modules\vite\bin\vite.js' build --config frontend/vite.config.ts
```

建议重点检查：

1. `GET /health` 返回的 `cache_path` 是否落在 `D:\New project 6`
2. 数据中心导入 / 补齐后，`Warehouse` 覆盖是否同步更新
3. 风险清单、红绿家数、强势板块是否能正常展示
4. 保存策略后，`运行产物\策略配置\saved-strategies.json` 是否生成
5. `C:\Users\大帝之资\Documents\New project 6` 是否仍是指向 `D:\New project 6` 的 Junction，而不是实际文件副本

## 12. 桌面端版本与安装包

### 12.1 当前桌面端版本

- 当前版本：`1.0.3`
- 本次更新内容：恢复“今日实时行情”和“资讯与事件”的原本职责；新增独立的“同花顺复盘总评 / 同花顺早盘总评”框架；桌面端安装包包含该前端更新。
- Tauri 配置版本：`src-tauri\tauri.conf.json`
- Rust 包版本：`src-tauri\Cargo.toml`
- 前端包版本：`package.json`

### 12.2 安装包位置

- 构建输出：`D:\New project 6\src-tauri\target\release\bundle\nsis\A股策略回测工作台_1.0.3_x64-setup.exe`
- 归档目录：`D:\New project 6\运行产物\安装包`

### 12.3 更新签名说明

`src-tauri\tauri.conf.json` 开启了 `createUpdaterArtifacts`，但生成 `.sig` 和 `latest.json` 需要匹配的 Tauri 私钥环境变量。若本机环境没有 `TAURI_SIGNING_PRIVATE_KEY`，仍可生成并安装 NSIS 安装包，但不能生成可被自动更新器使用的签名发布文件。

### 12.4 发布前准备清单

主功能验证通过后再做以下发布准备，不要提前手工改发布产物：

1. 确认版本号一致：`package.json`、`pyproject.toml`、`src-tauri\Cargo.toml`、`src-tauri\tauri.conf.json` 当前都应为 `1.0.3`。
2. 确认 `src-tauri\bin\astock-data-service.exe` 来自最新后端代码，发布前需重新执行 `npm run build:data-service` 或 `scripts\build-data-service.ps1`。
3. 确认 NSIS 安装包和 `.sig` 同时存在于 `src-tauri\target\release\bundle\nsis`。
4. 用真实 `.sig` 执行 `scripts\write-latest-json.ps1` 生成 `release-assets\latest.json`；不要手工把未来版本写进 `latest.json`。
5. 当前仓库里的 `release-assets\latest.json` 如果仍显示 `1.0.1`，表示它还是上一版发布清单，需等本次签名安装包生成后再更新。
6. GitHub Release 资产至少包含 NSIS 安装包和 `latest.json`，且 `latest.json.platforms.windows-x86_64.url` 指向同一个 Release tag 下的真实安装包。
7. 安装验证时确认应用内“检查更新”能读到 GitHub `releases/latest/download/latest.json`，签名校验通过后再对外发布。

## 13. 清理规则

可以安全清理的生成物：

1. `.pytest_cache`
2. `.pyinstaller`
3. `.tmp`
4. `.reference`
5. `dist`
6. `src-tauri\bin`
7. `src-tauri\gen`
8. `src-tauri\target`
9. `backend\astock_backtester.egg-info`
10. 各目录下的 `__pycache__`

不要清理：

1. `运行产物\本地数据仓`
2. `运行产物\策略配置`
3. `运行产物\签名密钥`
4. 用户仍在使用的安装包与桌面运行目录
5. `.tools` 与 `node_modules`，除非准备重新安装开发依赖
6. `tests` 和前端 `.test.tsx` / `.test.ts` 文件，它们是开发验证源码，不是运行缓存
