# Astock Backtester v1.3.5 Release Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the verified correctness fixes as v1.3.5 while removing the superseded full-array strategy command, obsolete local installers, and unreferenced historical agent documents.

**Architecture:** Keep the atomic per-strategy Tauri mutation contract as the only desktop write path. Align all seven release version sources, verify the signed updater chain from newly generated artifacts, and defer destructive local installer cleanup until the replacement is proven usable.

**Tech Stack:** Python 3.11/pytest/Ruff, React/TypeScript/Vitest/Vite, Rust/Tauri/Cargo, PowerShell, NSIS, GitHub CLI.

---

### Task 1: Remove The Deprecated Full-Array Desktop Mutation

**Files:**
- Modify: `tests/test_build_scripts.py`
- Modify: `frontend/src/savedStrategies.ts`
- Modify: `frontend/src/savedStrategyStore.test.ts`
- Modify: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: Write a failing source-boundary test**

Add a test that reads the frontend and Rust production sources and asserts neither `persist_saved_strategies` nor `persistSavedStrategiesToStore` is present, while `upsert_saved_strategy` and `delete_saved_strategy` remain registered.

- [ ] **Step 2: Run the boundary test and verify RED**

Run: `.\.tools\python-3.11.9\python.exe -m pytest tests/test_build_scripts.py -q -k deprecated`

Expected: failure because the old command and helper still exist.

- [ ] **Step 3: Remove only the superseded interface**

Delete the Rust command, its invoke registration, the frontend helper, stale comments, and tests that search for the old command. Preserve browser `persistSavedStrategies`, the load command, and atomic upsert/delete.

- [ ] **Step 4: Verify GREEN and zero references**

Run the targeted pytest test, `savedStrategyStore.test.ts`, and `rg -n 'persist_saved_strategies|persistSavedStrategiesToStore'` over source/test files.

Expected: tests pass and `rg` returns no matches.

### Task 2: Align Version 1.3.5

**Files:**
- Modify: `tests/test_build_scripts.py`
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `pyproject.toml`
- Modify: `backend/astock_backtester/__init__.py`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/Cargo.lock`
- Modify: `src-tauri/tauri.conf.json`

- [ ] **Step 1: Tighten the version test to expect 1.3.5**

Extend `test_release_manifests_use_one_version` to read package-lock root metadata and assert the sole version is exactly `1.3.5`.

- [ ] **Step 2: Run the test and verify RED**

Expected: failure showing current version `1.3.4`.

- [ ] **Step 3: Update every release version source**

Use the existing file formats and change only project version values from 1.3.4 to 1.3.5.

- [ ] **Step 4: Run the version test and verify GREEN**

Expected: one passing version-alignment test with no stale 1.3.4 project version in active manifests.

### Task 3: Remove Unreferenced Historical Agent Documents

**Files:**
- Delete: completed files under `docs/superpowers/plans/` dated before 2026-07-11
- Delete: completed feature specs under `docs/superpowers/specs/` dated before 2026-07-11
- Keep: `docs/release.md`, `docs/dev.md`, `docs/capital-flow-crawler-report.md`, `docs/architecture-optimization-audit.md`, the 2026-07-11 release spec and this plan

- [ ] **Step 1: Prove candidates have no inbound repository references**

Run fixed-string searches for every candidate path outside the candidate itself.

- [ ] **Step 2: Delete only zero-reference completed agent documents**

Use explicit literal paths. Do not recursively delete `docs`, and do not delete user-authored root Markdown files.

- [ ] **Step 3: Verify retained documentation and git diff**

Expected: current release design/plan and operational docs remain; only named historical agent documents are deleted.

### Task 4: Run Source Gates

- [ ] **Step 1: Run Python full suite with a unique basetemp**
- [ ] **Step 2: Run frontend full Vitest suite**
- [ ] **Step 3: Run direct bundled-Node TypeScript typecheck**
- [ ] **Step 4: Run Vite production build**
- [ ] **Step 5: Run Rust library tests with configured Cargo/Rustup paths**
- [ ] **Step 6: Compare changed-file Ruff findings against commit baseline**
- [ ] **Step 7: Run `git diff --check` and inspect all changed/untracked files**

Every command must exit zero except Ruff may retain existing baseline findings; it must introduce zero new findings.

### Task 5: Build And Verify Signed v1.3.5 Artifacts

- [ ] **Step 1: Run `build:data-service` and verify required sidecars exist**
- [ ] **Step 2: Load the signing key into process-local environment variables without printing it**
- [ ] **Step 3: Run the Tauri CI build and capture the true exit status**
- [ ] **Step 4: Verify fresh 1.3.5 installer and `.sig` timestamps, sizes, and hashes**
- [ ] **Step 5: Generate `release-assets/latest.json` from the real signature with ASCII release asset name**
- [ ] **Step 6: Validate updater JSON version, tag URL, signature, and asset name**
- [ ] **Step 7: Compare packaged sidecar/resource hashes and run the installed-package endpoint/NDJSON probe required by `AGENT必读.md`**

If any signing, packaging, hash, or probe check fails, stop before cleanup or GitHub publishing.

### Task 6: Clean Old Local Installers

- [ ] **Step 1: Resolve and verify the NSIS directory is inside `D:\New project 6\src-tauri\target\release\bundle\nsis`**
- [ ] **Step 2: Confirm the verified 1.3.5 installer and signature exist**
- [ ] **Step 3: Delete explicit 1.3.1 through 1.3.4 installer and `.sig` paths**
- [ ] **Step 4: List the directory and confirm only the 1.3.5 installer/signature remain**

Do not touch GitHub historical releases or `运行产物`.

### Task 7: Commit, Push, Tag, And Release

- [ ] **Step 1: Stage only manifests, source, tests, current docs, and explicit document deletions**
- [ ] **Step 2: Verify staged scope contains no memory files, binaries, keys, signatures, updater staging files, or runtime data**
- [ ] **Step 3: Commit as `release: prepare v1.3.5` and push the current branch**
- [ ] **Step 4: Create and push annotated tag `v1.3.5` at the verified commit**
- [ ] **Step 5: Create the non-draft GitHub Release and upload the ASCII installer plus `latest.json`**
- [ ] **Step 6: Query the release, compare uploaded asset size/digest, and verify `releases/latest/download/latest.json` reports 1.3.5**
- [ ] **Step 7: Report the commit, tag, release URL, verification totals, local cleanup result, and intentionally retained files**
