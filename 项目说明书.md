# A股策略回测工作台项目说明书

## 1. 项目目标

本项目的目标不是做一个只会展示静态页面的半成品，而是做一个可以真正落地使用的 A 股桌面回测工具，核心能力包括：

1. 基于本地历史数据完成全市场回测。
2. 支持用户手动编写入场条件、离场条件，并在运行前完成校验。
3. 支持仓位控制、止盈止损、持仓天数、股票池等关键参数调节。
4. 首页同时展示实时行情、强势板块、资讯、风险提示。
5. 采用“本地历史数据 + 实时接口增量补充”的混合模式，兼顾稳定性与时效性。

项目当前的工作根目录统一为：

- `D:\New project 6`

本地历史数据仓统一为：

- `D:\New project 6\运行产物\本地数据仓`

## 2. 总体架构

项目采用三层结构：

1. 前端界面层：React + TypeScript
2. 桌面容器层：Tauri（Rust）
3. 数据与回测层：Python

数据流如下：

1. 用户在前端页面设置策略、仓位、时间区间、股票池。
2. 前端通过 Tauri 命令拉起本地 Python 数据服务。
3. Python 数据服务读取本地数据仓，或调用实时接口补充市场快照、资讯、风险清单。
4. 回测执行器按条件逐日扫描历史数据，并流式返回阶段进度、交易明细和最终结果。
5. 前端实时展示交易进度、交易明细、收益概览和风险提示。

### 2.1 为什么采用这套结构

原因很直接：

1. Python 更适合做行情数据处理、Pandas 计算、回测逻辑、数据导入。
2. React 更适合做复杂交互界面、流式进度反馈、策略配置面板。
3. Tauri 比 Electron 更轻，桌面分发成本更低，更新机制也更清晰。
4. 把 Python 服务放在本地而不是远程，可以保证历史回测不依赖外网。

## 3. 目录结构

核心目录如下：

1. `backend`
   Python 后端与数据服务实现。
2. `frontend`
   React 前端页面与组件。
3. `src-tauri`
   Tauri 桌面容器、进程管理、更新配置。
4. `tests`
   Python 回测测试。
5. `运行产物`
   桌面软件、安装包、本地数据仓、签名密钥等运行时资产。

运行产物目录含义：

1. `运行产物\桌面软件`
   当前桌面程序可执行文件。
2. `运行产物\安装包`
   安装包与签名产物。
3. `运行产物\本地数据仓`
   历史行情、本地缓存、分区仓库。
4. `运行产物\签名密钥`
   Tauri 更新签名相关文件。

## 4. 依赖说明

### 4.1 前端依赖

主要使用：

1. `react`
2. `typescript`
3. `vite`
4. `vitest`
5. `lucide-react`

用途：

1. React 负责页面结构和状态管理。
2. Vite 负责本地开发和构建。
3. Vitest 负责前端单元测试。
4. lucide-react 提供统一图标。

### 4.2 Python 依赖

主要使用：

1. `pandas`
2. `numpy`
3. `pyarrow`
4. `pydantic`
5. `requests`
6. `beautifulsoup4`
7. `adata`

用途：

1. Pandas/Numpy 处理历史数据与指标计算。
2. PyArrow 存储和读取 parquet 数据。
3. Pydantic 约束接口模型与回测配置。
4. Requests/BeautifulSoup 拉取和解析实时接口、资讯接口。
5. `adata` 用于补足 A 股基础数据能力。

### 4.3 Rust/Tauri 依赖

主要使用：

1. `tauri`
2. `tauri-plugin-process`
3. `tauri-plugin-updater`

用途：

1. 拉起本地 Python 进程。
2. 承接前端调用。
3. 处理桌面更新和安装包信息。

## 5. 数据层设计

### 5.1 为什么做本地数据仓

只靠实时接口会有三个问题：

1. 历史跨度不稳定。
2. 回测速度受网络影响。
3. 同一策略重复跑结果不稳定。

所以历史回测必须以本地数据为主。

### 5.2 当前数据模式

项目当前采用双轨模式：

1. 历史回测：优先读取本地数据仓。
2. 实时首页：通过实时接口抓取最新指数、板块、新闻、风险，再叠加本地兜底。

### 5.3 本地数据仓结构

本地数据仓路径：

- `D:\New project 6\运行产物\本地数据仓`

内部大致分为两类：

1. `parquet`
   兼容旧缓存格式。
2. `warehouse`
   新版分区数据仓。

其中 `warehouse/daily_bars/year=YYYY/daily_bars.parquet` 用于按年份分区，原因是：

1. 全市场从 2015 年开始的数据量已经不小。
2. 按年切分能减少单文件体积，便于读取时间区间。
3. 后续增量更新时不需要重写整个总表。

## 6. 接口设计

### 6.1 Tauri 命令

Tauri 层主要暴露两个入口：

1. `ensure_data_service`
   启动或复用本地数据服务。
2. `backend_command`
   执行覆盖率等 CLI 型后端命令。

### 6.2 本地数据服务 HTTP 接口

当前核心接口包括：

1. `/ping`
   轻量健康探测。
2. `/health`
   返回本地缓存路径、服务端口、数据覆盖情况。
3. `/realtime/market-snapshot`
   返回实时指数、红绿家数、强势板块、昨日强势板块追踪。
4. `/market/news`
   返回市场资讯。
5. `/risk/alerts`
   返回风险清单。
6. `/strategy/conditions/validate`
   校验入场/离场条件文本。
7. `/strategy/recommended`
   返回推荐策略模板。
8. `/run/backtest`
   普通回测接口。
9. `/run/backtest/stream`
   流式回测接口。
10. `/sync/full-market`
   全市场数据补全任务。
11. `/sync/jobs/{job_id}`
   查询全市场同步进度。

### 6.3 实时数据来源

当前使用的是多源兜底：

1. 指数行情：Ashare/Sina
2. 强势板块：东方财富板块榜，失败时退回本地分组统计
3. 市场资讯：东方财富栏目、东方财富滚动页、财联社电报
4. 风险清单：本地观察名单 + 东方财富 + Sina + adata + 本地名称扫描

这样设计的原因是单一源不稳定，多源组合才能提高可用性。

## 7. 回测算法说明

### 7.1 回测执行逻辑

回测是按交易日逐日推进的，核心流程如下：

1. 读取时间区间内的历史日线。
2. 计算策略条件所需指标。
3. 逐日扫描股票池。
4. 先处理已有持仓的离场条件。
5. 再处理新的入场候选。
6. 生成买卖记录、权益曲线、回撤、胜率等指标。

### 7.2 为什么先离场再入场

原因是 A 股实际交易更接近这个顺序：

1. 先判断旧仓位是否需要卖出。
2. 再看今天还能不能买新票。

这样可避免仓位占用逻辑混乱。

### 7.3 T+1 约束

本项目明确按 A 股 T+1 规则处理，不允许当日买入当日卖出。这个约束直接体现在交易撮合逻辑里，用来避免回测结果虚高或逻辑失真。

### 7.4 仓位算法

现在已支持两种单股仓位模式：

1. `fixed_ratio`
   按总权益固定比例分配。
2. `equal_slots`
   按剩余可用仓位槽位均分。

同时增加了以下字段：

1. 计划成交金额
2. 实际买入金额
3. 实际卖出金额
4. 目标仓位占比
5. 实际仓位占比
6. 平均仓位
7. 最大仓位

这样做的原因是：

1. 回测收益不能只看“买了没买”，还要看“买了多少”。
2. A 股按 100 股整手交易，实际成交金额和理论金额会有偏差。
3. 没有仓位维度，回测结果没有实盘意义。

### 7.5 条件系统

策略条件支持两类：

1. 入场条件
2. 离场条件

用户现在可以输入文本条件，后端先解析、再校验。这样比单纯下拉选择更灵活，但为了避免乱填导致系统无法识别，增加了：

1. 条件样例
2. 运行前校验
3. 错误信息返回

### 7.6 流式回测

流式回测接口会持续回传：

1. 当前阶段
2. 当前进度
3. 新开仓交易
4. 平仓交易
5. 最终结果

这样前端可以边跑边显示，而不是全部跑完才一次性吐结果。

## 8. 首页模块说明

首页目前分成四块主能力：

1. 今日实时行情
2. 市场资讯
3. 风险提示
4. 回测与策略工作台

### 8.1 今日实时行情

展示：

1. 上证、深证、创业板指数
2. 红绿家数
3. 强势板块
4. 昨日强势板块追踪
5. 市场评价

### 8.2 市场资讯

展示：

1. 来自东方财富和财联社的市场新闻
2. 北京时间发布时间
3. 标签、情绪倾向

### 8.3 风险提示

风险模块不是只看当前回测股票，而是尽量做全市场潜在风险扫描，当前逻辑以本地观察名单为主，再叠加实时源补充，用于展示潜在 ST、退市预警及原因。

### 8.4 数据中心

数据中心负责：

1. 查看覆盖率
2. 补全缺失数据
3. 导入本地文件
4. 启动全市场下载
5. 查看同步日志和同步进度

## 9. 这次修复的关键问题

### 9.1 实时接口迟迟不连接的根因

这次排查出来的根因有两个：

1. 桌面程序运行的是调试版副本，启动本地服务时默认走系统 Python，而不是项目内置 Python。
2. 项目根目录下的 `.astock-cache` 还是旧项目的坏链接，指向一个已经不存在的目录。

结果就是：

1. 本地服务要么根本起不来。
2. 要么刚起就因为缓存路径无效而退出。

### 9.2 修复方式

已经做的修复包括：

1. Tauri 启动链优先使用 `D:\New project 6\.tools\python-3.11.9\python.exe`
2. Tauri 启动后端时使用绝对 `backend` 路径，而不是相对 `backend`
3. Tauri 启动 Python 时设置正确的工作目录为项目根
4. 重新修正 `.astock-cache` 链接到 `D:\New project 6\运行产物\本地数据仓`

修复后，桌面程序拉起的本地服务已经可以返回：

1. `/health`
2. `/realtime/market-snapshot`
3. `/market/news`
4. `/risk/alerts`

## 10. 当前验证结果

我已经完成的验证包括：

1. Python 后端测试通过
2. 前端测试通过
3. Rust/Tauri 测试通过
4. 桌面程序实际拉起本地服务成功
5. 本地服务实际返回实时行情、资讯、风险清单成功

其中实时接口验证结果包括：

1. `health` 返回本地数据覆盖
2. `realtime` 返回实时指数与本地板块兜底
3. `news` 返回东方财富新闻
4. `risk` 返回本地观察名单风险清单

## 11. 已实现的主要功能

当前已实现的主能力如下：

1. 本地历史数据仓
2. 全市场历史数据覆盖读取
3. 全市场同步任务与进度反馈
4. 回测参数设置
5. 入场条件/离场条件文本校验
6. 流式回测
7. 单股仓位控制
8. 收益、回撤、胜率、平均仓位、最大仓位展示
9. 交易明细展示
10. 首页实时行情
11. 市场资讯
12. 风险清单
13. 推荐策略模板
14. 桌面更新基础链路

## 12. 当前限制与后续建议

还有几项客观限制需要说明：

1. 某些实时源会偶发失败，所以前端需要继续保留多源兜底。
2. `capital_flow` 目前覆盖仍明显少于 `daily_bars`，这是后续补全的重点。
3. 当前桌面运行产物使用的是调试构建副本，若后续要稳定发版，建议再把 sidecar 打包链彻底修顺。
4. 风险名单目前以本地观察名单为主，后续可继续扩展更稳定的监管或公告接口。

建议的下一步优先级：

1. 继续补齐资金流和市值覆盖率。
2. 把 sidecar 打包链查透，避免未来只靠调试版副本运行。
3. 把回测条件语法再文档化，给用户更清晰的表达模板。
4. 增加每日收盘后板块复盘模块。

