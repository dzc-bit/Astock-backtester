# A股策略回测工作台项目说明书

## 1. 阅读地图与目标

本项目是一个真实可运行的 Windows 桌面版 A 股策略回测工作台，不是静态演示页。它的核心链路是：

1. 桌面端启动本地数据服务。
2. 数据中心补齐或导入历史数据，并写入本地仓库。
3. 回测引擎只依赖本地仓库完成可复现回测。
4. 首页用实时行情、新闻、复盘和风险模块辅助理解当日市场。
5. 发布时用 Tauri 打包桌面端、sidecar 和更新清单。

这条链路决定了本文的阅读顺序：先看路径边界，再看架构，再看数据从哪里来、存到哪里、被哪些接口和页面消费，最后看验证、发布和清理规则。

### 1.1 交付目标

当前交付目标包括：

1. 用本地历史数据完成全市场或指定股票池回测。
2. 支持用户录入入场条件、离场条件，并在运行前完成接口级校验。
3. 首页展示今日实时行情、行情评价、新闻汇总、资讯与事件、同花顺复盘/早盘总评和风险清单。
4. 回测完成后，在收益概览里突出展示 `latest_strategy_matches.matches` 的当日 user 模式候选；若只来自本地回测快照，前端必须标注非实时。
5. 所有业务数据统一留在 `D:\New project 6` 目录树，不迁到 `AppData`，不清理 `运行产物`。

### 1.2 文档分工

1. `README.md` 面向 GitHub 展示，只保留项目简介、核心能力、通用架构、主要接口和开发命令。
2. 本说明书负责记录本机路径、运行产物、按钮与接口映射、数据来源、模块边界、发布注意事项和清理边界。
3. 与本机安装路径、磁盘位置、签名密钥、安装包、数据仓、Junction 有关的内容，维护在本说明书或 `运行产物\目录说明.md`，不要放回 `README.md`。

## 2. 路径和运行产物边界

路径规则是后续所有章节的前提：数据中心、回测、策略保存、桌面安装和发布脚本都必须围绕同一个真实业务根目录工作。

### 2.1 真实业务根目录

- 真实业务根目录：`D:\New project 6`
- 文档入口：`C:\Users\大帝之资\Documents\New project 6`

`C:\Users\大帝之资\Documents\New project 6` 是指向 D 盘项目的 Junction，不保存第二份项目文件。所有命令、数据写入和发布产物说明都以 `D:\New project 6` 为准。

### 2.2 业务数据与运行产物目录

| 内容 | 路径 | 说明 |
| --- | --- | --- |
| 本地数据仓 | `D:\New project 6\运行产物\本地数据仓` | 桌面端正式历史数据目录 |
| 项目缓存联接 | `D:\New project 6\.astock-cache` | 指向本地数据仓的 Junction |
| 已保存策略 | `D:\New project 6\运行产物\策略配置\saved-strategies.json` | 桌面端正式策略配置 |
| 安装包 | `D:\New project 6\运行产物\安装包` | 发布或本机归档安装包 |
| 签名密钥 | `D:\New project 6\运行产物\签名密钥` | 更新签名相关密钥 |

### 2.3 路径约束

1. `.astock-cache` 只是入口，正式业务数据在 `运行产物\本地数据仓`。
2. `GET /health` 返回的 `cache_path` 应落在 `D:\New project 6` 目录树。
3. 发布态桌面端不把历史数据或策略配置当成 `AppData` 业务数据。
4. 桌面端保存策略时，正式落地文件是 `运行产物\策略配置\saved-strategies.json`。
5. 不在 C 盘保留项目副本；从“文档”目录打开项目时继续使用现有 Junction。
6. `D:\New project 6\运行产物\本地数据仓` 是主仓路径；旧 `D:\New project 6\运行产物\本地数据` 只允许只读兼容，不作为 UI、补齐或回测的活跃写入源。

## 3. 总体架构

路径明确后，可以把系统看成四层。每一层职责独立，通过明确接口连接，避免把行情、复盘、新闻、回测和桌面更新混在一起。

| 层级 | 目录 | 职责 |
| --- | --- | --- |
| 前端界面层 | `frontend/src` | React + TypeScript，负责页面状态、交互、展示和接口调用 |
| 桌面容器层 | `src-tauri/src` | Tauri + Rust，负责服务生命周期、文件路径、策略持久化、更新器和桌面命令 |
| 本地数据服务层 | `backend/astock_backtester/service.py` | 提供 `127.0.0.1:<port>` HTTP 接口，聚合数据、行情、资讯、复盘、风险和回测 |
| 数据与回测层 | `backend/astock_backtester` | 数据 provider、仓库、清洗、回测算法、策略校验和模型定义 |

运行链路：

1. 前端通过 Tauri 命令 `ensure_data_service` 拉起或复用本地服务。
2. Tauri 启动 sidecar `astock-data-service.exe` 或 Python 模块服务。
3. 前端通过 `http://127.0.0.1:<port>` 调用本地 HTTP 服务。
4. 数据服务读取 `Warehouse` 和 `LocalCache`，必要时访问公开上游。
5. 回测接口返回进度、交易事件和最终 `BacktestResult`。

这一层架构解释了下一节的数据生命周期：外部数据只在补齐、行情、资讯和复盘等 provider 内出现；回测本身必须回到本地仓库。

## 4. 数据生命周期

数据生命周期回答三个问题：数据从哪里下载、下载后写到哪里、哪些功能使用它。

### 4.1 本地数据形态

历史数据主要有两种形态：

1. `LocalCache`：旧缓存兼容层。
2. `Warehouse`：当前正式本地仓库。

当前实现规则：

1. 导入或补齐日线时，同时写入 `LocalCache` 和 `Warehouse`。
2. 覆盖率查询优先读 `Warehouse`。
3. 回测优先读 `Warehouse`。
4. `Warehouse` 不足时才退回 `LocalCache`。

### 4.2 为什么回测必须走本地仓

1. 回测结果要可复现，不能在回测过程中依赖外网波动。
2. 全市场数据量大，不能每次临时拉取。
3. 涨跌停、止盈止损、年化收益和今日命中个股都需要稳定的历史数据口径。
4. 实时行情可以辅助当日展示，但不能替代回测历史数据。

### 4.3 数据中心下载接口来源

数据中心有三个和数据写入有关的入口，它们的数据来源不同：

| 前端动作 | HTTP 接口 | 后端入口 | 上游来源 | 写入位置 |
| --- | --- | --- | --- | --- |
| 下载全市场历史数据 | `POST /sync/full-market` | `SyncJobManager.start_full_market(...)` | `CompositeProvider([ADataProvider(), AkshareProvider(), HttpAStockProvider()])` | `Warehouse` + `LocalCache` |
| 补全缺失数据 | `POST /fetch/daily-bars` | `fetch_daily_bars_into_cache(...)` | 同一个 `CompositeProvider`，逐个股票拉取并合并 | `Warehouse` + `LocalCache` |
| 导入示例或本地文件 | `POST /import/daily-bars` | `import_daily_bars_into_cache(...)` | 示例数据或本地 CSV | `Warehouse` + `LocalCache` |

`/sync/full-market` 和 `/fetch/daily-bars` 共用 provider 顺序：

1. 优先使用 `ADataProvider`：
   - 股票列表：`adata.stock.info.all_code()`
   - 日线行情：`adata.stock.market.get_market(...)`
   - 股本历史：`adata.stock.info.get_stock_shares(...)`
2. 如果 `adata` 失败或返回空数据，再使用 `AkshareProvider`：
   - 股票列表：`ak.stock_zh_a_spot_em()`
   - 日线行情：`ak.stock_zh_a_hist(...)`
   - 实时个股快照：`ak.stock_zh_a_spot_em()`，用于实时红绿家数候选源和后端 provider 字段归一化，不写入历史回测仓
   - 后端把中文列名清洗为 `symbol/trade_date/open/high/low/close/volume/amount/change_pct/change/turnover_rate`
3. 如果 AKShare 也失败或返回空数据，再使用 `HttpAStockProvider`。
4. `HttpAStockProvider` 调用 `AStockDataAdapter.from_http_sources()`。

`/fetch/daily-bars` 的 HTTP 来源：

1. 日线 K 线：百度股市通 / PAE `https://finance.pae.baidu.com/selfselect/getstockquotation`
2. 资金流：东方财富 `https://push2his.eastmoney.com/api/qt/stock/fflow/daykline/get`
3. 个股基础和市值信息：东方财富 `https://push2.eastmoney.com/api/qt/stock/get`

因此，当前下载数据已经不是单一路径：主链是 `adata`，第二层是 AKShare 封装的公开东方财富历史行情，最后才是项目原有 HTTP 回退。这样可以降低轻量直连东方财富 URL 被拦截时对数据中心补齐的影响，同时把 URL 与字段映射都留在后端 provider，前端只接收结构化 JSON。

1.1.1 同时纳入 `backend/astock_backtester/data/capital_flow_crawler.py` 作为独立资金流 crawler/provider 边界。它只读取东方财富资金流公开 XHR，返回结构化行和 `failures`，并对大陆 IP/风控常见的 `RemoteDisconnected` 尝试固定 header 变体重试。它不直接写 `Warehouse` 或 `LocalCache`；未来如果接入数据中心，应由更高层 backfill 服务决定如何把 `main_net_inflow` 按 `symbol + trade_date` 合并入主仓，并把失败原因返回 UI diagnostics。

### 4.4 覆盖日期和补齐规则

数据中心的日期框连接“本地已有数据”和“下一次补齐范围”：

1. `POST /coverage/daily-bars` 只查询覆盖，不下载。
2. `GET /health` 返回整体覆盖，供数据中心初始化状态。
3. 如果本地 `daily_bars` 已有全市场覆盖且结束日期早于最近开盘日，前端把覆盖结束日期作为补齐开始日期。
4. 例子：覆盖显示 `2015-01-05 至 2026-05-26`，当前为 2026-06-07 周日时，默认补齐范围为 `2026-05-26` 至 `2026-06-05`。
5. 如果没有可用覆盖或只是少量样例数据，前端回退到最近 5 个工作日；后端覆盖缺口计算使用 A 股交易日历，不把春节、清明、劳动节、国庆等合法休市日写入 `missing_trade_dates`。1.1.1 内置 2024、2025、2026 年休市区间，跨年发布前要按交易所公告刷新交易日历和覆盖测试。
6. 补齐成功后，前端用返回的覆盖信息重新计算下一次建议补齐范围，并刷新覆盖明细；如果覆盖已经到最近开盘日，日期框会回到最近 5 个工作日窗口，避免用户误以为补齐没有生效。
7. 如果上游因大陆 IP、限流、空响应或字段变化未返回近几日数据，界面保留当前覆盖范围和服务日志，不删除本地运行产物。
8. 如果某个年份 Parquet 分区损坏，`Warehouse` 读取覆盖和最近交易日时跳过该分区；当同一年新数据重新写入时，损坏分区会被新数据覆盖，避免“补齐接口返回成功但覆盖日期不变化”。
9. `.astock-cache` 是指向 `D:\New project 6\运行产物\本地数据仓` 的 Junction。旧 `D:\New project 6\运行产物\本地数据` 不在 Tauri 发布态候选目录中，不作为 UI、补齐或回测的活跃路径；发现旧目录残留时应在确认绝对路径后只删除该旧目录，不能清理整个 `运行产物`。

## 5. 桌面命令与本地服务接口

上一节讲数据如何进入本地仓；这一节讲前端如何启动服务并访问这些能力。

### 5.1 Tauri 命令

| 命令 | 入口 | 用途 |
| --- | --- | --- |
| `ensure_data_service` | `src-tauri/src/commands.rs` / `service_manager.rs` | 启动或复用本地数据服务，返回端口、基础 URL、缓存目录 |
| `backend_command` | `src-tauri/src/commands.rs` | 调用 Python CLI，主要用于浏览器预览或无数据服务时的回退路径 |
| `load_saved_strategies` | `src-tauri/src/commands.rs` | 读取 `运行产物\策略配置\saved-strategies.json` |
| `persist_saved_strategies` | `src-tauri/src/commands.rs` | 保存或删除自定义策略并写回 `saved-strategies.json` |

### 5.2 GET 接口

接口实现文件：`backend/astock_backtester/service.py`

| 接口 | 作用 | 后端实现 |
| --- | --- | --- |
| `GET /ping` | 轻量健康探测 | `do_GET` |
| `GET /health` | 返回缓存路径、端口、覆盖信息 | `build_service_health(...)` |
| `GET /logs/recent` | 最近日志 | `self.server.state.logs` |
| `GET /realtime/market-snapshot` | 首页行情快照 | `RealtimeMarketProvider.market_snapshot()` |
| `GET /market/news` | 首页资讯 | `MarketNewsProvider.latest_news()` |
| `GET /market/commentary` | 独立行情评价 | `MarketCommentaryProvider.current_commentary()` |
| `GET /market/news-summary` | 独立新闻汇总 | `MarketNewsSummaryProvider.latest_summary()` |
| `GET /market/fupan` | 同花顺复盘总评 | `MarketBriefingProvider.latest_fupan()` |
| `GET /market/zaopan` | 同花顺早盘总评 | `MarketBriefingProvider.latest_zaopan()` |
| `GET /risk/alerts` | 风险清单 | `RiskAlertProvider.current_alerts()` |
| `GET /strategy/recommended` | 推荐策略模板 | `recommended_strategies(...)` |
| `GET /sync/jobs/{job_id}` | 全市场同步进度 | `SyncJobManager.get_job(...)` |

### 5.3 POST 接口

| 接口 | 作用 | 后端实现 |
| --- | --- | --- |
| `POST /coverage/daily-bars` | 查询指定股票和日期区间覆盖细节 | `build_daily_bars_coverage(...)` |
| `POST /sync/full-market` | 启动全市场历史下载 | `SyncJobManager.start_full_market(...)` / `run_full_market(...)` |
| `POST /import/daily-bars` | 导入示例数据或本地文件 | `import_daily_bars_into_cache(...)` |
| `POST /fetch/daily-bars` | 补齐缺失行情 | `fetch_daily_bars_into_cache(...)` |
| `POST /run/backtest` | 普通回测 | `run_configured_backtest(...)` |
| `POST /run/backtest/stream` | 流式回测 | `_run_backtest_stream(...)` |
| `POST /strategy/conditions/validate` | 校验入场/离场条件文本 | `validate_condition_text(...)` / `validate_exit_condition_text(...)` |

### 5.4 关键接口契约

`GET /health` 是桌面端数据中心使用的完整健康检查。Tauri 主程序启动 sidecar 时只用轻量 `/ping` 判断进程已启动。`/health` 读取 `Warehouse` 覆盖信息时，如果单个 parquet 分区、历史缓存或本地依赖异常，接口必须继续返回 JSON：优先回退 `LocalCache.coverage()`，两者都不可读时返回空覆盖，不能让客户端出现“远程端关闭连接”。

`POST /run/backtest/stream` 的流式事件至少包括：

1. `progress`：扫描进度，包含交易日、已扫描天数、总天数、持仓数、已平仓数和候选数。
2. `trade_opened`：真实买入成交事件。
3. `trade_closed`：真实卖出成交事件。
4. `trade_blocked`：涨停阻买或跌停阻卖事件，`trade.blocked_reason` 必须说明阻断原因。
5. `result`：最终 `BacktestResult`，包含收益指标、权益曲线、交易列表、预检问题和 `latest_strategy_matches`。

## 6. 页面模块与接口关系

本节把上一节的接口映射到用户实际看到的页面。原则是：页面只消费结构化接口，不直接持有上游 URL；长正文、表格、链接和诊断信息进入弹窗或详情区，不撑满首页。

### 6.1 应用启动阶段

| 页面 / 组件 | 动作 | 前端函数 | 调用接口 | 后端入口 |
| --- | --- | --- | --- | --- |
| `App` / `DataCenter` | 启动即连接本地服务 | `ensureDataService(cacheDir)` | Tauri `ensure_data_service` | `DataServiceManager::ensure_running` |
| `App` | 首次加载推荐策略 | `loadRecommendedStrategies` | `GET /strategy/recommended` | `recommended_strategies(...)` |
| `App` | 首次加载资讯 | `loadMarketNews` | `GET /market/news` | `MarketNewsProvider.latest_news()` |
| `App` | 首次加载行情评价 | `loadMarketCommentary` | `GET /market/commentary` | `MarketCommentaryProvider.current_commentary()` |
| `App` | 首次加载新闻汇总 | `loadNewsSummary` | `GET /market/news-summary` | `MarketNewsSummaryProvider.latest_summary()` |
| `App` | 首次加载同花顺复盘总评 | `loadMarketBriefing(..., "fupan")` | `GET /market/fupan` | `MarketBriefingProvider.latest_fupan()` |
| `App` | 首次加载同花顺早盘总评 | `loadMarketBriefing(..., "zaopan")` | `GET /market/zaopan` | `MarketBriefingProvider.latest_zaopan()` |
| `App` | 首次加载风险 | `loadRiskAlerts` | `GET /risk/alerts` | `RiskAlertProvider.current_alerts()` |
| `App` | 首次加载行情 | `loadRealtimeMarketSnapshot` | `GET /realtime/market-snapshot` | `RealtimeMarketProvider.market_snapshot()` |
| `App` | 首次加载已保存策略 | `loadSavedStrategiesFromStore` | Tauri `load_saved_strategies` | 读取 `saved-strategies.json` |

### 6.2 数据中心

组件文件：`frontend/src/components/DataCenter.tsx`

| 按钮 / 模块 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 刷新覆盖范围 | `handleRefreshDetails` | `/coverage/daily-bars` | `POST` | 查询当前股票列表在日期区间内的行情、资金流、市值缺口 |
| 刷新覆盖范围后同步状态 | `refreshServiceState` | `/health` | `GET` | 刷新整体数据覆盖与缓存路径 |
| 刷新覆盖范围后同步日志 | `refreshLogs` | `/logs/recent` | `GET` | 刷新本地服务日志 |
| 下载全市场历史数据 | `handleFullMarketSync` | `/sync/full-market` | `POST` | 启动全市场导入任务 |
| 同步进度轮询 | `loadSyncJob` | `/sync/jobs/{job_id}` | `GET` | 每秒轮询一次进度 |
| 补全缺失数据 | `handleFetch` | `/fetch/daily-bars` | `POST` | 对当前股票列表补齐日线 |
| 导入示例数据 | `handleImportSample` | `/import/daily-bars` | `POST` | `source=sample` |
| 导入本地文件 | `handleImportFile` | `/import/daily-bars` | `POST` | `source=file, path=<文件路径>` |

数据中心规则见第 4 节。这里强调页面行为：补齐失败时保留已有覆盖和服务日志；补齐成功时覆盖日期应同步更新；不清理 `运行产物`。

### 6.3 首页行情、资讯与复盘

组件文件：

- `frontend/src/components/MarketDashboard.tsx`
- `frontend/src/components/MarketCommentaryPanel.tsx`
- `frontend/src/components/NewsSummaryPanel.tsx`
- `frontend/src/components/NewsPanel.tsx`
- `frontend/src/components/TonghuashunBriefingPanel.tsx`
- `frontend/src/components/RiskAlertsModal.tsx`

| 模块 / 按钮 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 今日实时行情 | `loadRealtimeMarketSnapshot` | `/realtime/market-snapshot` | `GET` | 指数、红绿家数、强势板块、昨日强势追踪统一走这里 |
| 行情评价 | `loadMarketCommentary` | `/market/commentary` | `GET` | 独立展示结论、主线、风险、明日观察；实时失败时可使用同花顺复盘公开页生成回顾 |
| 新闻汇总 | `loadNewsSummary` | `/market/news-summary` | `GET` | 独立展示新闻主题、要点、风险和来源条数 |
| 资讯与事件 | `refreshNews` | `/market/news` | `GET` | 支持定时刷新与手动刷新；主界面保留滚动列表，不折叠剩余新闻 |
| 同花顺复盘总评 | `loadMarketBriefing(baseUrl, "fupan")` | `/market/fupan` | `GET` | 独立卡片，不嵌入今日实时行情 |
| 同花顺早盘总评 | `loadMarketBriefing(baseUrl, "zaopan")` | `/market/zaopan` | `GET` | 独立卡片，不嵌入资讯列表 |
| 查看复盘/早盘全文 | `setOpenKind(...)` | 无新增接口 | 无 | 展示首次加载时后端已返回的 `sections`、`links`、`tables` 和诊断信息 |
| 同花顺原文按钮 | `<a href={briefing.source_url}>` | 同花顺网页 | 外链 | 打开真实 `source_url` |
| 风险提示弹窗刷新 | `refreshRiskAlerts` | `/risk/alerts` | `GET` | ST / 退市风险清单 |

布局边界：

1. `MarketDashboard` 只负责“今日实时行情”。
2. `MarketCommentaryPanel` 只负责“行情评价”。
3. `NewsSummaryPanel` 只负责“新闻汇总”。
4. `NewsPanel` 只负责“资讯与事件”。
5. `TonghuashunBriefingPanel` 单独承载“同花顺复盘总评”和“同花顺早盘总评”。
6. 主界面只显示精要；完整主线、全部主题、长文正文、表格、链接和诊断信息进入“查看全文”弹窗或内部详情区。
7. `NewsPanel` 是例外：资讯与事件本身就是滚动式新闻流，主界面不再显示“还有 N 条资讯已收起”，而是在固定高度内滚动浏览全部 `/market/news` 返回项。

### 6.4 策略配置和回测结果

组件文件：

- `frontend/src/components/StrategyWorkbench.tsx`
- `frontend/src/components/RecommendedStrategies.tsx`
- `frontend/src/components/ResultsOverview.tsx`

| 模块 / 按钮 | 前端函数 | 调用接口 | 方法 | 说明 |
| --- | --- | --- | --- | --- |
| 校验入场条件 | `onValidateCondition` | `/strategy/conditions/validate` | `POST` | `mode=entry` |
| 校验离场条件 | `validateExitCondition` | `/strategy/conditions/validate` | `POST` | `mode=exit` |
| 套用推荐策略 | `onApply(item.strategy)` | 无 | 无 | 推荐策略数据来自 `/strategy/recommended` |
| 套用已保存策略 | `onApplySavedStrategy` | 无 | 无 | 纯前端状态覆盖当前策略 |
| 删除已保存策略 | `persistSavedStrategiesToStore` | Tauri `persist_saved_strategies` | invoke | 写回 `saved-strategies.json` |
| 套用数据中心日期 | `applyCoverageDates` | 无 | 无 | 直接把覆盖日期写入回测设置 |
| 运行历史回测 | `runBacktest` | `/run/backtest/stream` | `POST` | 桌面端主路径 |
| 当日符合用户策略股票 | `latest_strategy_matches.matches` | `/run/backtest/stream` 最终 result | `POST` | 展示代码、名称、本地收盘价或未来实时候选价、涨跌幅、匹配理由、rank_score |
| 回测完成后保存策略提示 | `queueStrategySavePrompt(...)` | 无 | 无 | 页面内提示，不使用 `alert` / `confirm` |

策略配置依赖数据中心的覆盖日期，回测结果依赖本地仓库，今日 user 模式候选依赖 `/run/backtest/stream` 最终 `result.latest_strategy_matches` 或未来独立候选 provider。这三者是同一条本地数据链路，不应塞进复盘正文，也不应在前端直接拼接上游 URL 或爬虫逻辑。

## 7. 行情、资讯、复盘和风险来源

第 4 节说明历史数据下载来源；本节说明首页行情和资讯来源。它们可以访问外网，但只服务展示和分析，不能替代本地回测仓库。

### 7.1 实时行情 `GET /realtime/market-snapshot`

后端文件：`backend/astock_backtester/data/realtime.py`

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 指数行情 | `_fetch_indexes()` | Sina `https://hq.sinajs.cn/list=...` |
| 强势板块主链 | `_fetch_ths_concept_section_rows()` | 同花顺概念题材页 `q.10jqka.com.cn/gn/` |
| 强势板块一级回退 | `_fetch_ths_industry_html_rows()` | 同花顺行业页 `q.10jqka.com.cn/thshy/...` |
| 强势板块二级回退 | `_fetch_sina_sectors()` | Sina 板块页 |
| 强势板块本地追踪辅助 | `_fetch_ths_hot_topic_rows()` | 同花顺热点归因 |
| 红绿家数主链 | `_fetch_live_breadth()` | 同花顺市场总览 / XHR |
| 红绿家数一级回退 | `_fetch_sina_breadth()` | Sina 个股实时行情批量请求 |
| 红绿家数二级回退 | `_fetch_tencent_breadth()` | Tencent 个股实时行情批量请求 |
| 红绿家数三级回退 | `_fetch_akshare_breadth()` | AKShare `stock_zh_a_spot_em()` |
| 红绿家数重型兜底 | `_fetch_heavy_breadth()` | 公开 HTML/XHR，必要时 Playwright headless DOM |
| 红绿家数本地兜底 | `_snapshot_from_local()` | 本地 `Warehouse` 最近交易日 |
| 昨日强势追踪 | `_snapshot_from_local()` + `_local_market_groups()` | 同花顺/新浪板块成员线索 + 本地历史涨跌 |

实时行情约束：

1. 强势板块不再使用东方财富 `push2.eastmoney.com/api/qt/clist/get` 作为实时主链。
2. 红绿家数必须经过 provider 层完整性校验：`total < 3000` 直接失败；当本地最近股票池不少于 3000 只时，还必须达到本地股票池 65% 以上，否则写入 `diagnostics` 并继续下一个来源。
3. `total=192`、`total=26`、或虽超过 3000 但显著低于本地股票池合理比例的局部数据，不能标记为全市场宽度，也不能让行情评价生成“实时盘面数据完整”的结论。
4. 东方财富轻量 spot 只保留为显式开启的备选：必须有超时、字段校验、数量校验和 `diagnostics`，不能作为唯一主链。
5. 重型公开行情 provider 先尝试公开 HTML/XHR，再尝试 Playwright headless DOM；固定超时、快速失败、缓存最近成功宽度，不做登录、cookie 池、代理池、验证码绕过或付费抓取。
6. 红绿家数在快照流程中优先于强势板块读取；强势板块源有独立时间预算，超时后先返回指数、红绿家数和本地题材兜底，不让首页一直等待慢接口。
7. 同花顺、Sina、Tencent、AKShare 或重型 provider 不可用时快速回退最近成功快照或本地最近交易日，不拖慢首页刷新。
8. 非交易日、午间、收盘后和上游失败时，前端保留最近成功快照，降低刷新频率，轻量提示“刷新中/使用最近数据/非交易时段/实时接口暂不可用”。
9. 所有降级原因写入 `diagnostics`。

### 7.2 行情评价 `GET /market/commentary`

后端文件：`backend/astock_backtester/data/market_commentary.py`

| 数据项 | 后端函数 | 来源 |
| --- | --- | --- |
| 基础盘面 | `current_commentary()` | `RealtimeMarketProvider.market_snapshot()` |
| 新闻催化辅助 | `_news_theme_text()` | `MarketNewsProvider.latest_news(limit=12)` |
| 主线驱动 | `MarketCommentaryPoint(...)` | 强势板块、红绿家数、新闻标签聚合 |
| 风险与明日观察 | `current_commentary()` | 指数涨跌、市场宽度、强势题材延续情况 |
| 快照超时保护 | `_read_snapshot()` | 默认最多等待实时快照 10 秒 |
| 复盘兜底 | `_briefing_fallback()` | 实时快照失败、超时或不完整时读取 `MarketBriefingProvider.latest_fupan()` |
| 新闻降级兜底 | `_news_fallback()` | 实时行情和复盘均不可用时返回防守口径和新闻线索 |

前端显示规则：

1. 行情评价独立于“今日实时行情”，不改写实时行情卡片。
2. 主界面只显示一句结论、最多 1 个主线、1 个风险、2 条观察。
3. 完整依据、完整主线、风险、明日观察和诊断进入“查看全文”。
4. 新闻只能辅助，不能包装成确定行情结论。
5. 行情评价按严格状态机降级：盘中完整实时快照；午间/收盘后最近成功完整快照；非交易日最近交易日回顾；实时失败时最近成功完整快照；本地最近交易日；同花顺复盘；最后才是新闻辅助。
6. 若实时快照超过 10 秒、接口失败或字段不完整，`/market/commentary` 先尝试同花顺复盘公开页：返回 `ths-fupan+briefing-commentary`、`post_close`、`neutral` 口径，并把“实时盘面读取失败，已用同花顺复盘总评生成行情评价”写入诊断。
7. 只有复盘也不可用或只返回空入口时，才退回 `news-fallback+commentary`。新闻只能作为线索候选，不能包装成确定行情结论；若红绿家数不完整，诊断必须说明原因，不能出现“实时盘面数据完整”的结论。

### 7.3 新闻汇总和资讯

`GET /market/news` 后端文件：`backend/astock_backtester/data/news.py`

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 东方财富栏目资讯 | `_fetch_eastmoney_columns()` | `np-listapi.eastmoney.com/comm/web/getNewsByColumns` |
| 东方财富要闻页 | `_fetch_eastmoney_rolling()` | `finance.eastmoney.com/yaowen.html` |
| 财联社电报 | `_fetch_cailianpress()` | `www.cls.cn/nodeapi/telegraphList` |

`GET /market/news-summary` 后端文件：`backend/astock_backtester/data/news_summary.py`

| 数据项 | 后端函数 | 来源 |
| --- | --- | --- |
| 新闻原始列表 | `latest_summary()` | `MarketNewsProvider.latest_news(limit=24)` |
| 主题分组 | `_theme_keys()` | 新闻 tags + 标题/摘要关键词 |
| 主题情绪 | `_theme_sentiment()` | 主题内新闻 sentiment 计数 |
| 主题摘要 | `_theme_summary()` | 代表新闻与来源集合 |
| 重点标题 | `highlights` | 最近发布时间排序后的新闻标题 |
| 风险标题 | `risks` | negative sentiment 或 ST / 退市标签 |

新闻汇总主界面只展示前 2 个主题，每个最多 2 条短要点；完整主题摘要、全部要点、风险和诊断只在弹窗中展示。

资讯与事件不同于新闻汇总：它是按时间滚动的原始新闻流，主界面保留固定高度和内部滚动，不再折叠第 7 条以后的新闻。这样既不改变首页网格尺寸，也能让用户直接下滑查看全部近期事件。

### 7.4 同花顺复盘和早盘

后端文件：`backend/astock_backtester/data/briefing.py`

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 同花顺复盘总评 | `latest_fupan()` / `_parse_fupan()` | `https://stock.10jqka.com.cn/fupan/` |
| 同花顺早盘总评 | `latest_zaopan()` / `_parse_zaopan()` | `https://stock.10jqka.com.cn/zaopan/` |
| 复盘/早盘链接 | `_links_from_node()` | 同花顺页面内相对链接与绝对链接 |
| 文章详情展开 | `_expand_article_links()` / `_fetch_article_section()` | `stock.10jqka.com.cn/*.shtml` |
| 复盘/早盘表格 | `_table_from_node()` | 同花顺页面内表格 |
| 复盘公开行情兜底 | `_market_fallback_sections()` | Sina 指数与东方财富 A 股行情 |

复盘/早盘显示和清洗规则：

1. `/market/fupan` 和 `/market/zaopan` 由 `TonghuashunBriefingPanel` 独立消费，不混入今日实时行情、资讯列表或 user 模式候选。
2. “查看全文”不重新请求接口，展示后端首次加载时已返回的结构化内容。
3. “打开同花顺原文”必须真实打开 `source_url` 或文章链接；无链接则禁用并说明。
4. 早盘和复盘正文按段落展示，不把整坨长文本直接铺开。
5. 表格列名无法从原文获得时，后端先推断中性列名，前端再做第二层可读化；可识别的个股表格必须整理为“个股/涨幅/现价”，不能误标成题材原因。
6. 后端过滤纯 `%`、`% %`、重复时间戳、纯数字表格，以及“中文标签 + 连续数字/点位/涨跌额/涨跌幅”的统计噪声；可解析为行情表格时整理为 `名称 / 最新值 / 涨跌额 / 涨跌幅`。
7. 正文解析会优先按 `p/li/h1-h3` 分段；遇到同花顺页面把正文塞进 `div/span` 时，后端只读取叶子节点并逐行过滤数字汤，避免整块 `行业 + 神秘数字 + 重复时间戳` 混入全文。
8. user 模式候选不再追加到复盘正文；候选只来自 `/run/backtest/stream` 的 `latest_strategy_matches.matches` 或未来独立后端候选 provider。
9. 东方财富只保留在复盘公开行情兜底说明和最后线索，不作为 user 模式候选的前置依赖。

### 7.5 风险清单 `GET /risk/alerts`

后端文件：`backend/astock_backtester/data/risk.py`

| 数据项 | 后端函数 | 上游来源 |
| --- | --- | --- |
| 本地潜在风险观察名单 | `_alerts_from_local_watchlist()` | `potential_risk_watchlist.csv` |
| 东方财富风险源 | `_alerts_from_eastmoney()` | 东方财富 ST / 退市名称列表 |
| 新浪实时名称扫描 | `_alerts_from_sina_local_symbols()` | Sina 简版行情名称 |
| AData 兜底 | `_alerts_from_adata_all_codes_with_timeout()` | `adata.stock.info.all_code()` |
| 本地仓库兜底 | `_alerts_from_local()` | `Warehouse` 最近交易日与 `is_st` 字段 |

### 7.6 公开接口和爬虫限制

这些限制贯穿实时行情、资讯、复盘、风险和数据下载：

1. `mpquant/Ashare` 的可复用思路是 Sina/Tencent 双源切换和 `DataFrame` 规范化；本项目仅借鉴公开行情兜底策略，不直接引入依赖。
2. `akfamily/akshare` 的可复用思路是东方财富 `stock_zh_a_spot_em`、历史行情与清洗字段映射；本项目已增加 `AkshareProvider`，并用它为数据中心历史补齐和红绿家数备选提供统一后端封装，不把 URL 写入前端。
3. `simonlin1212/a-stock-data v3.2.2` 明确记录过百度 PAE 个别接口失效、改用东方财富接口，以及部分大陆住宅 IP 访问东方财富 push2/search-api 可能出现连接级风控、`HTTP 000` 或 `passportWeb` 空响应；本项目同样需要把这类上游异常写入 `diagnostics`。
4. 中国大陆网络、运营商出口、住宅 IP 或代理环境可能触发同花顺、Sina、腾讯、东方财富的 401/403、空响应、限流或字段变更。
5. 公开行情兜底只能辅助展示和复盘，不保证 SLA；若需要稳定生产级数据源，应接入有授权的数据服务，而不是继续扩大网页抓取范围。
6. 爬虫和公开接口只允许封装在后端 provider 内；React 前端只能消费结构化 JSON。
7. 所有降级必须写入 `diagnostics`，并在 UI 中以“公开行情兜底/使用本地最近交易日/接口暂不可用”等轻量提示表达。
8. 不得把新闻或公开涨幅榜包装成确定行情结论。
9. 重型爬虫 provider 只处理公开可访问 XHR/HTML 和必要时的 headless DOM，不做登录态维持、cookie 池、代理池、验证码绕过、付费抓取或前端 URL 暴露。

## 8. 回测和策略口径

上一节的实时行情和资讯用于解释市场；本节的回测用于验证策略。二者必须分开：回测输入来自本地历史数据，实时行情只做当日展示和复盘线索。

### 8.1 user 模式候选字段

`BacktestResult.latest_strategy_matches` 是回测完成后展示“当日符合用户策略个股”的正式数据源。该字段来自 `/run/backtest/stream` 最终 `result`，不能混进同花顺复盘正文。

字段说明：

1. `latest_strategy_matches.signal_date`：命中信号日期。
2. `latest_strategy_matches.trade_date`：用于展示的交易日期。
3. `latest_strategy_matches.matches[]`：当日符合用户策略的股票列表。
4. `matches[].symbol/name/close/change_pct/reasons/rank_score`：前端卡片展示字段。
5. 前端保留 `matched_stocks` 兼容回退，但正式字段以 `latest_strategy_matches.matches` 为准。
6. `trade_date` 等于上海时区今天时，标题显示“今日 user 模式候选”；否则显示“本地最近交易日候选”，避免历史回测结果被误读为今日实盘命中。
7. 当前字段只有本地 `close/change_pct` 时，列名和文案必须明确“本地收盘价/本地最近收盘价/非实时”；未来若接入独立候选 provider，可优先用实时行情补充现价和涨跌幅，失败再回退本地最近交易日。
8. 候选卡片按 `rank_score` 降序展示，保留代码、名称、价格字段、涨跌幅和最多 4 条匹配理由；长原因在卡片内部换行，不撑开首页，尽量补股票名称，避免大量显示 `--`。
9. 收益曲线区独立显示“历史权益曲线”和回测区间，曲线绘图区保留固定高度，避免被标题或候选卡片压缩。

### 8.2 回测成交和指标规则

1. `settings.limit_up_blocks_buy` 默认开启；若次日买入日开盘价接近涨停价，后端不买入，发出 `trade_blocked` 事件，并在 `blocked_reason` 写入“次日开盘接近涨停，未买入：股票代码”。
2. `settings.limit_down_blocks_sell` 默认开启；若卖出日开盘价接近跌停价，后端暂不卖出，保留持仓，发出 `trade_blocked` 事件，并在 `blocked_reason` 写入“卖出日开盘接近跌停，暂不卖出：股票代码”。
3. 涨跌停判断基于 `pre_close` 与开盘价：ST 股票按 5%，创业板 `300` 和科创板 `688` 按 20%，其余股票按 10%；缺少 `pre_close` 或开盘价时不做阻断。
4. `buy_price` 和 `sell_price` 是实际成交价，不再只代表计划价。
5. 开启 `conservative_execution` 时，买入价按开盘价加滑点，卖出价按触发价或开盘价扣滑点。
6. 止盈或止损触发时，卖出原始价优先取止盈 / 止损触发价；普通离场取卖出日开盘价，再统一扣除滑点、手续费和印花税。
7. `annualized_return_pct` 使用权益曲线首尾交易日自然日间隔计算：`(final_equity / initial_cash) ** (365 / days) - 1`；区间不足 2 个权益点或天数为 0 时为 0。
8. `trade_count`、胜率和平均单笔收益只统计已平仓交易；涨停阻买产生的 0 股事件不计入盈利交易。

## 9. 策略保存与持久化

策略保存连接前端策略状态和第 2 节的运行产物目录。

### 9.1 内置策略

内置策略定义在 `frontend/src/savedStrategies.ts`，当前保留：

1. 基础均衡策略
2. 放量突破策略
3. 回踩均线策略

### 9.2 用户保存策略

桌面端行为：

1. `App` 启动时通过 Tauri `load_saved_strategies` 读取 `saved-strategies.json`。
2. 回测运行成功且用户确认保存时，通过 Tauri `persist_saved_strategies` 写回该文件。
3. 删除已保存策略也写回同一个文件。

浏览器预览 / 单测回退：

1. 非 Tauri 运行时仍允许使用 `localStorage`。
2. 这只是预览与测试回退，不作为桌面端正式数据存储规则。

## 10. 测试与验证

验证命令用于确认前面各节的边界没有被破坏：路径仍指向 D 盘，本地仓覆盖正常，行情和复盘可降级，回测算法口径可信。

推荐验证命令：

```powershell
python -m pytest tests/test_data_operations.py tests/test_data_service_http.py tests/test_build_scripts.py -q
npm run test:ui -- --run
```

桌面端 1.1.1 专项验证命令：

```powershell
python -m pytest tests/test_engine.py tests/test_market_briefing.py tests/test_market_commentary.py tests/test_news_summary.py tests/test_data_service_http.py
& 'D:\New project 6\.tools\node-v20.18.1-win-x64\node.exe' 'D:\New project 6\node_modules\vitest\vitest.mjs' run --config frontend/vitest.config.ts
& 'D:\New project 6\.tools\node-v20.18.1-win-x64\node.exe' 'D:\New project 6\node_modules\typescript\bin\tsc' --noEmit
& 'D:\New project 6\.tools\node-v20.18.1-win-x64\node.exe' 'D:\New project 6\node_modules\vite\bin\vite.js' build --config frontend/vite.config.ts
cargo test --manifest-path src-tauri\Cargo.toml
npm run build:data-service
npm run tauri -- build --ci
```

重点检查：

1. `GET /health` 返回的 `cache_path` 是否落在 `D:\New project 6`。
2. 数据中心导入 / 补齐后，`Warehouse` 覆盖是否同步更新。
3. 风险清单、红绿家数、强势板块是否能正常展示。
4. 保存策略后，`运行产物\策略配置\saved-strategies.json` 是否生成。
5. `C:\Users\大帝之资\Documents\New project 6` 是否仍是指向 `D:\New project 6` 的 Junction。

## 11. 版本、安装包与发布

发布章节连接前面的代码、数据服务、Tauri sidecar 和更新签名。只有验证通过后，才应进入发布准备。

### 11.1 当前桌面端版本

- 当前版本：`1.1.1`
- Tauri 配置版本：`src-tauri\tauri.conf.json`
- Rust 包版本：`src-tauri\Cargo.toml`
- 前端包版本：`package.json`

本次更新内容：

1. 红绿家数 provider 增加完整性校验，拒绝 `total < 3000` 或显著低于本地股票池比例的局部宽度，所有失败原因写入 `diagnostics`。
2. 实时行情来源链扩展为同花顺市场总览/XHR、Sina/Tencent 批量、AKShare、重型公开行情 provider；东方财富轻量 spot 只能作为受控备选。
3. 行情评价按严格状态机降级，红绿家数不完整时不能生成确定实时结论，新闻只能作为线索候选。
4. 同花顺复盘/早盘继续保持独立模块，原文按钮使用真实 `source_url` 或文章链接，正文继续清洗数字汤、重复时间戳和 `% %`，个股表格识别为“个股/涨幅/现价”。
5. user 模式候选从复盘正文移出，只在回测结果 `latest_strategy_matches.matches` 展示；本地回测快照明确标注非实时。
6. `/coverage/daily-bars` 使用 A 股交易日历，不把合法休市日误报为缺失交易日。
7. 桌面安装后必须验证 sidecar hash、一组 HTTP 端点和 `/run/backtest/stream` 最终 `type=result`，不能只看安装器退出码。

### 11.2 安装包位置

| 内容 | 路径 |
| --- | --- |
| 构建输出 | `D:\New project 6\src-tauri\target\release\bundle\nsis\A股策略回测工作台_1.1.1_x64-setup.exe` |
| 归档目录 | `D:\New project 6\运行产物\安装包` |
| 桌面本地安装路径 | `C:\Users\大帝之资\AppData\Local\A股策略回测工作台` |
| 发布态 sidecar | 安装目录下的 `bin\astock-data-service.exe` |

发布态 sidecar 必须与 `D:\New project 6\src-tauri\bin\astock-data-service.exe` 保持同一构建。

2026-06-07 1.1.1 验证记录由本轮构建和安装后探针产生；若缺少 `TAURI_SIGNING_PRIVATE_KEY`，只能记录“安装包已生成但签名发布阻塞”，不得生成、上传或复用假的 `latest.json`。

### 11.3 更新签名说明

`src-tauri\tauri.conf.json` 开启了 `createUpdaterArtifacts`，但生成 `.sig` 和 `latest.json` 需要匹配的 Tauri 私钥环境变量。若本机环境没有 `TAURI_SIGNING_PRIVATE_KEY`，仍可生成并安装 NSIS 安装包，但不能生成可被自动更新器使用的签名发布文件。

发布规则：

1. v1.1.1 正式发布时必须同步完成 Git 提交、`v1.1.1` tag、GitHub Release 资产和本次真实签名生成的 `latest.json`。
2. 覆盖安装后必须确认桌面端加载的是本轮前端静态资源和本轮 `bin\astock-data-service.exe`。
3. 同版本 NSIS 覆盖时，sidecar 可能不会自动替换；如安装目录里的 sidecar 仍旧，应先停止桌面端和本地数据服务，再复制最新 sidecar。
4. 本地更新完成后重点验证 `/ping`、`/health`、`/realtime/market-snapshot`、`/market/commentary`、`/run/backtest/stream`。
5. 只有在签名安装包、`.sig` 和 `latest.json` 都由正式流程生成并验证后，才能走应用内“检查更新”或 GitHub Release 发布。

### 11.4 发布前准备清单

1. 确认版本号一致：`package.json`、`pyproject.toml`、`src-tauri\Cargo.toml`、`src-tauri\tauri.conf.json`、`backend\astock_backtester\__init__.py` 都应为 `1.1.1`。
2. 确认 `src-tauri\bin\astock-data-service.exe` 来自最新后端代码。
3. 发布前重新执行 `npm run build:data-service` 或 `scripts\build-data-service.ps1`。
4. 确认 NSIS 安装包和 `.sig` 同时存在于 `src-tauri\target\release\bundle\nsis`，并且二者来自同一次 `npm run tauri -- build --ci`。
5. 用真实 `.sig` 执行 `scripts\write-latest-json.ps1` 生成新的 `latest.json`。
6. 不要手工把未来版本写进 `latest.json`。
7. 不要复用旧 `.sig` 重新生成或发布 `latest.json`。
8. GitHub Release 资产至少包含 NSIS 安装包和 `latest.json`。
9. 发布验证时确认 GitHub `releases/latest/download/latest.json` 可公开读取，版本、签名和安装包 URL 均与本地发布清单一致。

## 12. 清理规则

清理规则收束全文：源码、测试和运行产物是项目资产；缓存、构建输出和临时目录才是可清理对象。

### 12.1 可以安全清理的生成物

1. `.pytest_cache`
2. `.pyinstaller`
3. `.tmp`
4. `.reference`
5. `dist`
6. `src-tauri\bin`
7. `src-tauri\gen`
8. `src-tauri\target`
9. `backend\astock_backtester.egg-info`
10. 各目录下的 `__pycache__`

### 12.2 不要清理

1. `运行产物\本地数据仓`
2. `运行产物\策略配置`
3. `运行产物\签名密钥`
4. 用户仍在使用的安装包与桌面运行目录
5. `.tools` 与 `node_modules`，除非准备重新安装开发依赖
6. `tests` 和前端 `.test.tsx` / `.test.ts` 文件，它们是开发验证源码，不是运行缓存
